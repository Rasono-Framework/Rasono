### [2026-07-10T17:40:00Z] | Repository hygiene, contributor philosophy, and framework-only Git preparation
- **Contexte :** Le projet devait etre prepare pour un push propre comme un vrai framework. Le dossier n'etait pas encore un depot Git, des repertoires temporaires de scaffolding trainaient encore a la racine, le `.gitignore` etait trop faible, et la philosophie du framework n'etait pas encore formalisee pour les futurs contributeurs.
- **Modifications effectuees :**
    - Suppression des repertoires temporaires `.tmp-rasono-*` qui ne faisaient pas partie du framework.
    - Renforcement de `.gitignore` pour exclure les artefacts locaux et non-framework (`.tmp-*`, `.agents/`, `agent/`, `.env*`, coverage, logs, repertoires temporaires).
    - Enrichissement du `README.md` avec une section `Contributor Philosophy`, une clarification du perimetre du repository et un renvoi vers `CONTRIBUTING.md`.
    - Creation de `CONTRIBUTING.md` pour expliciter la philosophie architecturale, le scope du repository, les regles de design, la hygiene Git et le niveau de qualite attendu.
- **Decisions Techniques :** Le repository doit representer uniquement le framework et ses actifs officiels. La documentation de contribution a donc ete traitee comme une piece d'architecture, pas comme un accessoire. Le choix a ete de rendre la philosophie visible a deux niveaux: dans le `README.md` pour le positionnement global, et dans `CONTRIBUTING.md` pour la discipline de contribution.
- **Impacts & Dependances :** Fichiers touches : `.gitignore`, `README.md`, `CONTRIBUTING.md`, `ENGINEERING_LOG.md`. Repertoires supprimes : `.tmp-rasono-api-cli`, `.tmp-rasono-custom-flags`, `.tmp-rasono-landing-cli`, `.tmp-rasono-modular`. Cette tranche prepare proprement l'initialisation Git et limite le risque de pousser des artefacts hors scope.
- **Prochaines etapes :** Initialiser le depot Git, configurer `origin`, creer une branche propre, valider l'etat du tree, puis pousser uniquement les fichiers du framework.

### [2026-07-10T17:05:00Z] | Phase 2 tranche 8: scaffolder data-aware et README de positionnement du framework
- **Contexte :** Le scaffolder officiel savait deja guider le choix Web/API, mais il ne savait pas encore guider proprement le choix `provider + database`. En parallele, le framework n'avait toujours pas de `README.md` racine capable d'expliquer sa philosophie, sa difference strategique et sa promesse produit.
- **Modifications effectuees :**
    - Evolution de `packages/create-rasono/src/index.ts` pour ajouter les flags `--data` et `--database`, un questionnaire guide en anglais pour choisir le provider data et la base, ainsi que la validation des combinaisons supportees.
    - Ajout du support de generation pour les couples suivants : `drizzle + postgres/mysql/sqlite/turso`, `kysely + postgres/mysql/sqlite/mssql`, `engine + turso`, ou aucun provider.
    - Generation conditionnelle des dependances data dans `apps/api/package.json`, d'un fichier `apps/api/src/data/index.ts` conforme au choix effectue, et d'un `apps/api/.env.example` qui documente explicitement les variables d'environnement attendues.
    - Ajout de tests d'integration dans `packages/testing/test/testing.test.mjs` pour verifier le cas nominal data-aware, le rejet d'une combinaison invalide, et l'aide CLI enrichie.
    - Creation de `README.md` a la racine pour formaliser la philosophie du framework, sa differentiation, ses objectifs et sa proposition de valeur.
- **Decisions Techniques :** Trois references ont fixe la direction. `create-t3-app` confirme la valeur d'un scaffolder modulaire pilotant des choix de stack. Prisma confirme qu'un projet credible doit traiter plusieurs bases comme des citoyens de premiere classe. Kysely confirme l'interet d'une histoire multi-dialecte explicite. Drizzle et Turso ont ensuite servi a figer des exemples de wiring officiels sans inventer de pseudo-ORM Rasono. La synthese retenue est donc : coeur neutre, provider choisi explicitement, base choisie explicitement, dependances seulement si necessaires.
- **Impacts & Dependances :** Fichiers touches : `packages/create-rasono/src/index.ts`, `packages/testing/test/testing.test.mjs`, `README.md`, `ENGINEERING_LOG.md`. Cette tranche renforce la credibilite de `create-rasono` comme point d'entree framework-grade et clarifie le positionnement global de Rasono pour les futurs utilisateurs et contributeurs.
- **Prochaines etapes :** Faire evoluer le scaffolder vers des starters data plus complets avec conventions de migrations, idempotency/outbox et un starter PostgreSQL de reference, puis eventuellement ajouter un provider officiel `@rasono/data-prisma` si cela reste coherent avec le principe de neutralite.

### [2026-07-10T16:10:00Z] | Phase 2 tranche 7: CLI create-rasono guidee, flags-first, et integralement en anglais
- **Contexte :** Le scaffolder officiel devait devenir plus framework-grade. Il savait deja generer de bons starters, mais l'experience CLI n'etait pas encore au niveau attendu: texte mixte, parcours interactif incomplet, et choix d'installation pas assez explicites pour un create command credible.
- **Modifications effectuees :**
    - Evolution de `packages/create-rasono/src/index.ts` pour supporter un vrai flux guide en anglais: choix du dossier cible, choix du package manager, choix du preset, confirmation des features une par une, puis confirmation d'installation des dependances.
    - Consolidation du mode flags-first avec `--preset`, `--pm`, `--api`, `--web`, `--actions`, `--swagger`, `--rpc`, `--install`, `--no-install`, `--yes`, `-y`, `--interactive`, `--help`, `-h`.
    - Normalisation de toute la sortie CLI utilisateur en anglais, y compris le resume final et le texte d'aide.
    - Ajout de tests d'integration dans `packages/testing/test/testing.test.mjs` pour couvrir a la fois le chemin non interactif et le chemin interactif avec reponses pas a pas.
- **Decisions Techniques :** Trois references ont oriente cette tranche: `create-next-app` pour le dual mode flags + questionnaire, `create-t3-app` pour le choix etape par etape des briques a installer, et `create-vite` pour un scaffolding rapide avec options scriptables. Rasono adopte donc la meme posture: flux guide par defaut pour la clarte, mais automatisable completement par flags pour les CI et les equipes avancees.
- **Impacts & Dependances :** Fichiers touches : `packages/create-rasono/src/index.ts`, `packages/testing/test/testing.test.mjs`, `ENGINEERING_LOG.md`. Cette tranche ameliore fortement l'ergonomie du scaffolder sans changer l'architecture des starters generes, et reduit le risque d'installations implicites ou mal comprises.
- **Prochaines etapes :** Ajouter des choices data/database au scaffolder (Drizzle, Kysely, Engine, PostgreSQL, Turso) avec la meme philosophie: coeur neutre, providers explicites, aucun couplage force.

### [2026-07-10T15:35:00Z] | Phase 2 tranche 6: provider officiel data-kysely pour la neutralite multi-base
- **Contexte :** La neutralite data du framework devait etre prouvee par plus qu'un discours. Apres `@rasono/data-drizzle` et `@rasono/data-engine`, il fallait un provider officiel explicitement multi-base pour montrer que PostgreSQL, MySQL, SQLite et MSSQL restent des chemins first-class sans faire de Rasono un ORM maison.
- **Modifications effectuees :**
    - Creation du package `@rasono/data-kysely` avec `package.json`, `tsconfig.json` et `src/index.ts`.
    - Ajout de `createKyselyDataAdapter()` avec support d'un client ou d'une factory, d'un `isolationLevel` explicite, d'une `configureTransaction()` avancee et d'une fermeture par `destroy()` si disponible.
    - Raccordement du package au monorepo dans `tsconfig.base.json`, `packages/create-rasono/src/index.ts` et `packages/testing/package.json`.
    - Ajout de tests runtime dans `packages/testing/test/testing.test.mjs` pour le chemin nominal, l'isolation explicite, la configuration transactionnelle avancee, l'erreur claire si `setIsolationLevel()` n'est pas supporte, et la fermeture par defaut via `destroy()`.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter Kysely comme provider first-class au meme niveau que Drizzle et Engine.
- **Decisions Techniques :** Trois references ont guide cette tranche: Kysely pour sa structure par dialectes et son API transactionnelle avec `transaction()` / `setIsolationLevel()` / `startTransaction()`, Drizzle pour confirmer la valeur d'un thin-provider qui n'efface pas l'API native, et Prisma pour rappeler qu'un framework credible doit accepter plusieurs backends de facon officielle. Le provider Rasono reste donc tres mince et n'essaie ni d'abstraire les dialectes, ni de simuler les savepoints lui-meme.
- **Impacts & Dependances :** Fichiers touches : `packages/data-kysely/package.json`, `packages/data-kysely/tsconfig.json`, `packages/data-kysely/src/index.ts`, `tsconfig.base.json`, `packages/create-rasono/src/index.ts`, `packages/testing/package.json`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/template/RASONO_AI.md`, `ENGINEERING_LOG.md`. Cette tranche renforce la promesse de neutralite database/ORM du framework et prepare un starter PostgreSQL plus convaincant.
- **Prochaines etapes :** Ajouter un starter de reference PostgreSQL + Drizzle ou Kysely avec idempotency/outbox, puis envisager `@rasono/data-prisma` si l'on veut couvrir aussi le segment DX enterprise autour de Prisma.

### [2026-07-10T15:05:00Z] | Phase 2 tranche 5: facade Engine proprietaire sans casser la neutralite du framework
- **Contexte :** Le package `@rasono/data-engine` avait un meilleur branding que `data-libsql`, mais sa facade publique restait encore trop proche de l'implementation sous-jacente (`libsql`). Il fallait clarifier un point d'architecture essentiel: Rasono construit un framework neutre vis-a-vis de l'ORM et de la base, meme si un provider officiel proprietaire existe.
- **Modifications effectuees :**
    - Evolution de `packages/data-engine/src/index.ts` pour introduire des symboles produit et neutres: `createEngineDataAdapter`, `createEngineClientFactory`, `EngineTransactionMode`, `EngineTransactionLike`, `EngineTransactionalClientLike`, `EngineClientFactory`, `CreateEngineDataAdapterOptions`, `CreateEngineClientFactoryOptions`.
    - Conservation des alias historiques `createTursoDataAdapter`, `createLibsqlDataAdapter` et `createTursoLibsqlClientFactory` afin d'eviter toute regression de compatibilite.
    - Mise a jour des tests runtime dans `packages/testing/test/testing.test.mjs` pour valider la facade `Engine` et la compatibilite des alias precedents.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour affirmer explicitement que `@rasono/data-engine` est optionnel, que `@rasono/data` reste le centre de gravite, et que PostgreSQL reste un citoyen de premiere classe du framework.
- **Decisions Techniques :** Trois references ont confirme la direction: Prisma documente plusieurs connecteurs officiels et base son experience sur le choix explicite du provider de base de donnees; Kysely structure sa neutralite autour des dialectes; Drizzle documente plusieurs drivers et runtimes par base, en particulier PostgreSQL, MySQL et SQLite. Rasono adopte donc la meme posture strategique: coeur neutre, providers explicites, facade produit optionnelle.
- **Impacts & Dependances :** Fichiers touches : `packages/data-engine/src/index.ts`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/template/RASONO_AI.md`, `ENGINEERING_LOG.md`. Cette tranche reduit le couplage percu a Turso/libSQL sans casser l'existant, et clarifie la promesse plateforme pour les equipes PostgreSQL et multi-ORM.
- **Prochaines etapes :** Ajouter des providers officiels `@rasono/data-kysely` et/ou `@rasono/data-prisma`, puis livrer un starter de reference PostgreSQL + Drizzle pour montrer que l'Engine n'est qu'une option parmi plusieurs chemins first-class.

### [2026-07-10T14:40:00Z] | Phase 2 tranche 4: primitives idempotency et outbox pour ecritures critiques
- **Contexte :** Le durcissement transactionnel precedent securisait mieux les commits/rollbacks, mais cela ne suffisait pas encore pour des workflows critiques exposes au reseau. Un framework credible doit aussi couvrir deux risques majeurs de production: la repetition d'une commande mutante apres timeout/retry client, et le dual-write fragile entre base de donnees et publication d'evenement.
- **Modifications effectuees :**
    - Extension de `packages/data/src/index.ts` avec des contrats minimaux `IdempotencyStore`, `OutboxStore`, `IdempotencyRecord`, `OutboxMessage` et des helpers `executeIdempotentOperation()` / `drainOutboxMessages()`.
    - Ajout de `serializeStoredOperationError()` pour persister des erreurs operationnelles sans stack ni details sensibles.
    - Ajout de `defineIdempotencyStore()` et `defineOutboxStore()` pour garder la meme ergonomie declarative que le reste de `@rasono/data`.
    - Ajout de tests runtime dans `packages/testing/test/testing.test.mjs` pour couvrir replay idempotent, conflit de fingerprint, persistence d'echec, drain d'outbox et preservation de l'erreur primaire si la release outbox echoue aussi.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter la regle d'usage framework-grade: fingerprint stable, pas de retry implicite, outbox dans la meme transaction que l'ecriture metier.
- **Decisions Techniques :** Trois references ont pilote cette tranche avant implementation: Stripe pour la discipline idempotency-key + fingerprint + replays deterministes, Medusa pour l'idee de recovery/resume sur operations longues et retriables, AWS Prescriptive Guidance pour l'elimination du dual-write via transactional outbox. L'implementation Rasono reste volontairement minimale: elle ne stocke rien elle-meme et n'impose ni table SQL, ni broker, ni ORM. Elle fournit seulement les contrats et le chemin d'orchestration surs.
- **Impacts & Dependances :** Fichiers touches : `packages/data/src/index.ts`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/template/RASONO_AI.md`, `ENGINEERING_LOG.md`. Cette tranche augmente fortement la credibilite de `@rasono/data` pour des use-cases paiement, billing, ordre ou workflow distribue, tout en preservant le coeur adapter-first.
- **Prochaines etapes :** Ajouter un starter de reference avec schema SQL recommande pour idempotency/outbox, conventions d'idempotency key HTTP, et un exemple complet sur Turso/Drizzle avec outbox worker et idempotency conflict mapping vers `409`.

### [2026-07-10T14:05:00Z] | Phase 2 tranche 3: surface Turso officielle et durcissement transactionnel
- **Contexte :** L'integration Turso/libSQL etait fonctionnelle mais encore inachevee sur deux points structurants: le naming public ne reflectait pas la cible produit (`data-libsql` etait trop bas niveau) et la gestion des erreurs transactionnelles devait etre durcie pour eviter toute perte de cause primaire en cas d'echec compose (`work`, `commit`, `rollback`, `close`).
- **Modifications effectuees :**
    - Finalisation initiale de la surface publique Turso, ensuite renommee vers `@rasono/data-engine` pour adopter un naming plus proprietaire et plus framework-grade.
    - Evolution de `packages/data-engine/src/index.ts` pour exposer `createTursoDataAdapter()` comme API publique orientee produit, tout en gardant `createLibsqlDataAdapter` comme alias de compatibilite.
    - Durcissement du chemin transactionnel Turso/libSQL avec preservation de l'erreur primaire et aggregation explicite des echecs secondaires de `rollback` et `close`.
    - Durcissement complementaire de `packages/data/src/index.ts` pour que le fallback transactionnel generique ne perde plus la vraie cause si le rollback eche aussi.
    - Normalisation des commentaires d'en-tete touches sur le format anglais `Purpose / Goal / Value`.
- **Decisions Techniques :** Trois references officielles ont ete reprises avant stabilisation: Prisma pour la discipline autour des transactions interactives courtes et de l'isolation stricte, Drizzle pour la logique de transaction fine sans abstraction parasite, et Turso/libSQL pour les modes `read/write/deferred`, la fermeture obligatoire en `finally` et la semantique de `close()` qui rollback si necessaire. Aucun retry automatique n'a ete ajoute au framework: sur des flux financiers, relancer une transaction de facon implicite peut dupliquer des effets metier non idempotents. La responsabilite de retry reste donc explicite au niveau applicatif.
- **Impacts & Dependances :** Fichiers touches : `packages/data/src/index.ts`, `packages/data-drizzle/src/index.ts`, `packages/data-engine/src/index.ts`, `tsconfig.base.json`, `packages/testing/package.json`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/RASONO_AI.md`, `ENGINEERING_LOG.md`. Cette tranche aligne mieux la promesse produit Turso, ameliore l'auditabilite des incidents transactionnels et reduit le risque de diagnostic faux en production.
- **Prochaines etapes :** Ajouter ensuite un starter data de reference complet avec idempotency keys, outbox/event log et conventions de retry applicatif pour les use-cases critiques, afin de couvrir les besoins finance-grade au-dela de la seule mecanique SQL transactionnelle.

### [2026-07-10T13:17:03Z] | Phase 2 tranche 2: premier provider officiel data-drizzle
- **Contexte :** La couche `@rasono/data` etait volontairement minimale. Il fallait maintenant valider le modele thin-provider propose: integrer un ORM de reference sans le masquer ni faire glisser Rasono vers un pseudo-ORM maison. La question etait donc de choisir un premier provider officiel compatible avec l'objectif de modularite et de scalabilite.
- **Modifications effectuees :**
    - Ajout du package `packages/data-drizzle` exposant `@rasono/data-drizzle`.
    - Ajout de `createDrizzleDataAdapter()` dans `packages/data-drizzle/src/index.ts` avec support d'un client Drizzle natif, d'options de transaction et d'un hook de fermeture de session.
    - Integration du nouveau package dans `tsconfig.base.json`, `packages/create-rasono/src/index.ts` et `packages/testing/package.json`.
    - Ajout d'un test dans `packages/testing/test/testing.test.mjs` pour verifier qu'un client Drizzle-like reste utilise tel quel dans la transaction Rasono.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter l'architecture thin-provider et la premiere integration officielle.
- **Decisions Techniques :** Trois references ont ete comparees avant implementation : Prisma pour ses transactions interactives et ses options de timeout/isolation, Drizzle pour sa transaction callback fine et son positionnement leger/serverless-friendly, Kysely pour son controle avance avec savepoints. Drizzle a ete retenu comme premier provider officiel car il correspond le mieux a une integration fine qui n'ecrase pas l'API ORM native et reste compatible avec plusieurs backends, y compris libsql/Turso.
- **Impacts & Dependances :** Fichiers touches : `packages/data-drizzle/package.json`, `packages/data-drizzle/tsconfig.json`, `packages/data-drizzle/src/index.ts`, `tsconfig.base.json`, `packages/testing/package.json`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/RASONO_AI.md`. Cette tranche confirme que Rasono peut integrer un provider concret sans gonfler le coeur `@rasono/data`.
- **Prochaines etapes :** Ajouter un starter de reference data-end-to-end, puis enchainer sur `@rasono/data-prisma` ou `@rasono/data-kysely` selon la cible produit. Ensuite, brancher les migrations natives du provider choisi plutot que d'inventer une couche maison.

### [2026-07-10T12:32:20Z] | Phase 2 tranche 1: couche data officielle
- **Contexte :** La Phase 1 est suffisamment close pour attaquer la Phase 2 sur une premiere brique de production. Le manque le plus structurant etait l'absence d'une story data officielle: request-scoped session, transactions explicites et wiring propre des repositories sans couplage a un ORM unique.
- **Modifications effectuees :**
    - Ajout du package `packages/data` avec `@rasono/data`, expose comme couche data officielle adapter-agnostic.
    - Ajout de `defineDataAdapter()`, `createDataSessionDep()`, `defineRepository()` et `createRepositoryDep()` dans `packages/data/src/index.ts`.
    - Integration du package dans `tsconfig.base.json`, `packages/testing/package.json` et `packages/create-rasono/src/index.ts`.
    - Ajout d'un test d'integration dans `packages/testing/test/testing.test.mjs` pour verifier session request-scoped, commit, rollback et repository wiring.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter la couche data officielle.
- **Decisions Techniques :** La conception s'inspire de trois references benchmarkees avant implementation : FastAPI/SQLModel pour la session par requete et le cleanup via dependency, NestJS pour le caractere database-agnostic et l'injection de repository, Spring pour les transactions explicites et fiables. Rasono n'en copie aucune a l'identique : il adapte ces patterns a son modele DI request-scoped et a son exigence adapter-first.
- **Impacts & Dependances :** Fichiers touches : `packages/data/package.json`, `packages/data/tsconfig.json`, `packages/data/src/index.ts`, `tsconfig.base.json`, `packages/testing/package.json`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/RASONO_AI.md`. Cette tranche ouvre la voie a une couche data plus serieuse sans imposer TypeORM, Prisma ou SQLModel comme dependance de base.
- **Prochaines etapes :** Etendre Phase 2 avec migrations/outillage provider-specifique, unit-of-work plus riche, conventions de transactions sur use-cases et eventuellement une integration officielle pour une stack de reference (par exemple Prisma ou SQL).

### [2026-07-10T12:27:32Z] | Phase 1 socle: analyse AST stricte et erreurs localisees
- **Contexte :** Une partie de la CLI reposait encore sur des heuristiques regex pour extraire des metadata de route, ce qui etait trop fragile pour un framework qui veut des erreurs precises et une discipline de contrat forte. Il fallait aussi veiller a ce que ce durcissement n'ajoute pas de cout sur le hot path runtime.
- **Modifications effectuees :**
    - Ajout de `packages/cli/src/analyzeRoute.ts` avec une analyse AST TypeScript stricte des exports `defineRoute()` / `defineApi()`.
    - Remplacement de la detection regex de `method` dans `packages/cli/src/generateRpc.ts` par l'analyse AST, avec messages d'erreur `fichier:ligne:colonne`.
    - Ajout d'un test CLI dans `packages/testing/test/testing.test.mjs` pour verifier qu'une route invalide remonte bien une erreur localisee a la ligne exacte.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter le durcissement AST et la promesse de debug plus rapide.
- **Decisions Techniques :** L'analyse AST reste strictement cote CLI/build afin de ne pas penaliser la production. Le choix de TypeScript AST evite les faux positifs des regex et fournit des positions de source fiables sans alourdir le runtime HTTP.
- **Impacts & Dependances :** Fichiers touches : `packages/cli/src/analyzeRoute.ts`, `packages/cli/src/generateRpc.ts`, `packages/cli/package.json`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/template/RASONO_AI.md`. Cette tranche renforce la robustesse du codegen et la qualite du retour d'erreur sans toucher aux performances des requetes.
- **Prochaines etapes :** Finir la validation de la tranche OpenAPI/AST, puis enchainer sur une couche d'observabilite de reference pour fermer proprement la Phase 1.

### [2026-07-10T12:22:11Z] | Phase 1 socle: OpenAPI plus riche et reusable
- **Contexte :** La generation OpenAPI existait deja, mais elle etait encore largement embarquee dans la CLI et restait trop minimale pour porter une vraie posture framework-grade. Il fallait rendre cette couche plus riche, plus stricte, et surtout reutilisable depuis le coeur du framework.
- **Modifications effectuees :**
    - Extension de `packages/app/src/route-definitions.ts` avec des metadata OpenAPI plus expressives: exemples, descriptions de paramètres, request body documente, `externalDocs` et exemples de reponse/erreur.
    - Ajout de `packages/app/src/openapi.ts` pour centraliser la construction du document OpenAPI depuis des routes declaratives Rasono au lieu de laisser cette logique uniquement dans le code genere de la CLI.
    - Export public du builder via `packages/app/src/index.ts`.
    - Simplification de `packages/cli/src/generateApi.ts`, qui s'appuie maintenant sur `buildOpenApiDocument()` du framework et ne re-implemente plus toute la logique de spec dans le fichier auto-genere.
    - Ajout d'un test direct dans `packages/testing/test/testing.test.mjs` pour verifier les metadata OpenAPI riches: descriptions de paramètres, exemples request/response, erreurs custom documentees, `externalDocs`, `security` et extension `x-rasono-authz`.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter l'usage officiel de `openapi` sur `defineRoute()`.
- **Decisions Techniques :** Le builder OpenAPI a ete place dans `@rasono/app` plutot que dans la CLI pour faire de la spec un vrai contrat de framework reutilisable par d'autres outils (SDK, docs, checks) et non un simple artefact de generation. Les metadata restent optionnelles et additives, ce qui preserve la compatibilite avec les routes existantes.
- **Impacts & Dependances :** Fichiers touches : `packages/app/src/route-definitions.ts`, `packages/app/src/openapi.ts`, `packages/app/src/index.ts`, `packages/cli/src/generateApi.ts`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/template/RASONO_AI.md`. Cette tranche reduit le risque de derive entre contrat de route, document OpenAPI et futurs outils de generation client.
- **Prochaines etapes :** Terminer la sortie de Phase 1 avec une couche d'observabilite de reference, puis attaquer la Phase 2 autour de data layer officiel, jobs/workflows et outillage de production.

### [2026-07-10T12:18:13Z] | Phase 1 socle: CLI de generation module et policy
- **Contexte :** Les conventions de modules et de policies etaient maintenant assez bien definies dans le framework et les starters, mais la CLI ne savait encore generer qu'une page. Tant que ces conventions ne sont pas outillees officiellement, Rasono reste trop dependant de la discipline manuelle du repo.
- **Modifications effectuees :**
    - Ajout de `packages/cli/src/generateScaffold.ts` pour porter une generation dediee de modules et de policies avec commentaires d'en-tete anglais, structure officielle (`module`, `service`, `api/index.ts`) et mise a jour du registre `src/modules/index.ts`.
    - Evolution de `packages/cli/src/index.ts` pour exposer `rasono generate module <name>` et `rasono generate policy <module> <name>`, en conservant les commandes existantes.
    - Ajout d'un test CLI dans `packages/testing/test/testing.test.mjs` pour verifier la generation reelle d'un module `billing`, d'une policy `require-admin`, et la mise a jour du registre de modules.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter les nouvelles commandes et leur structure de sortie.
- **Decisions Techniques :** La generation reste volontairement minimaliste mais opinionated: un module genere sa frontiere, un service d'entree et une route de base afin de donner immediatement une structure coherente sans surcharger les projets. La policy est generee separement pour garder une autorisation declarative et composable, plutot qu'un couplage implicite au module scaffold initial.
- **Impacts & Dependances :** Fichiers touches : `packages/cli/src/generateScaffold.ts`, `packages/cli/src/index.ts`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/template/RASONO_AI.md`. Cette tranche reduit l'ecart entre les conventions recommandees du framework et les gestes de creation quotidiens.
- **Prochaines etapes :** Continuer la fin de Phase 1 avec une OpenAPI plus riche et/ou une observabilite de reference, maintenant que la CLI commence a imposer la structure officielle.

### [2026-07-10T12:08:54Z] | Phase 1 socle: base de tenant isolation
- **Contexte :** L'authZ declarative etait en place, mais il manquait encore une base explicite pour l'isolation inter-tenant. Sans ca, Rasono restait vulnerable au risque classique de confusion de contexte tenant si les handlers oublient des checks d'appartenance ou de perimetre.
- **Modifications effectuees :**
    - Extension de `packages/core/src/runtime.ts` pour ajouter `tenantId` au `Principal` officiel et documenter le fichier avec un commentaire d'en-tete anglais.
    - Evolution du scaffolder `packages/create-rasono/src/index.ts` pour generer des principals de reference enrichis avec `tenantId`, exposer des variables d'environnement `RASONO_*_TENANT_ID`, et ajouter une route `auth/api/tenant.ts` demonstrant une policy d'isolation tenant.
    - Enrichissement de `auth.policies.ts` genere avec `requireTenantAccess()` afin d'offrir un point de depart officiel pour les checks d'appartenance tenant.
    - Ajout de tests dans `packages/testing/test/testing.test.mjs` pour verifier l'isolation tenant au runtime et la presence des artefacts tenant dans le starter genere.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter le pattern `principal.tenantId` + policies explicites.
- **Decisions Techniques :** La tranche reste volontairement minimale: un `tenantId` unique sur le principal suffit a introduire une frontiere de confiance claire sans imposer encore un modele multi-tenant plus lourd (claims, scopes complexes, tenant memberships multiples). Les checks d'isolation restent explicites dans les policies pour preserver la lisibilite et eviter une magie implicite difficile a auditer.
- **Impacts & Dependances :** Fichiers touches : `packages/core/src/runtime.ts`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/RASONO_AI.md`, `packages/testing/test/testing.test.mjs`. Cette tranche renforce la securite applicative du framework en donnant une voie officielle pour eviter les fuites inter-tenant.
- **Prochaines etapes :** Transformer cette base en patterns plus riches de resource policies (owner + tenant), puis faire refléter plus precisement ces contraintes de securite dans l'OpenAPI et la generation SDK.

### [2026-07-10T12:03:45Z] | Phase 1 socle: policies d'autorisation declaratives
- **Contexte :** L'auth de reference du starter couvrait deja l'authentification multi-strategie, mais la couche d'autorisation restait essentiellement limitee a `auth.required` et `roles`. Il fallait ajouter une vraie primitive framework pour les policies/guards fines sans retomber dans des middlewares implicites ou couples au transport.
- **Modifications effectuees :**
    - Extension de `packages/app/src/route-definitions.ts` avec `RoutePolicy`, `policy` sur `defineRoute()`, `definePolicy()` et `composePolicies()` pour exprimer des checks d'autorisation reutilisables au niveau contrat de route.
    - Export public de ces primitives via `packages/app/src/index.ts`.
    - Evolution du scaffolder `packages/create-rasono/src/index.ts` pour generer `src/modules/auth/auth.policies.ts` et brancher les routes `admin`, `service` et `session` sur des policies explicites en plus des roles.
    - Mise a jour de `packages/testing/test/testing.test.mjs` avec un test runtime d'autorisation fine basee sur le `principal` + l'input de route, ainsi qu'un test de generation du starter auth-ready avec policies.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter le pattern officiel `resolvePrincipal()` + `definePolicy()` / `composePolicies()`.
- **Decisions Techniques :** Les policies retournent `false` pour signaler un refus standardise en `403`, mais elles peuvent toujours lever une erreur explicite si un cas plus riche est necessaire. La route continue a separer l'authN (`resolvePrincipal()`), l'authZ coarse-grained (`auth.required`, `roles`) et l'authZ fine (`policy`) afin de garder des frontieres de confiance lisibles et un modele mental simple.
- **Impacts & Dependances :** Fichiers touches : `packages/app/src/route-definitions.ts`, `packages/app/src/index.ts`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/RASONO_AI.md`, `packages/testing/test/testing.test.mjs`. Cette tranche renforce la posture securite en evitant que des checks d'autorisation sensibles restent caches dans les handlers.
- **Prochaines etapes :** Etendre la story authZ vers des policies de tenant isolation et enrichir l'OpenAPI/SDK pour mieux refléter les schemes et contraintes de securite exposes par les routes.

### [2026-07-10T11:46:26Z] | Phase 1 socle: auth de reference pour le starter API
- **Contexte :** La tranche precedente avait rendu Rasono plus credible sur les conventions de modules et la surface HTTP, mais il manquait encore une auth de reference "framework-grade" pour montrer comment brancher proprement `resolvePrincipal()` dans un projet genere sans retomber dans un middleware implicite ou un couplage Hono-specifique.
- **Modifications effectuees :**
    - Extension de `packages/auth/src/index.ts` avec `createSessionPrincipalResolver()` afin de couvrir le troisieme flux d'auth de reference du starter, en plus de `bearer` et `apiKey`.
    - Evolution du scaffolder `packages/create-rasono/src/index.ts` pour generer un service d'auth de reference (`src/modules/auth/auth.service.ts`), un module `auth`, des routes protegees (`me`, `admin`, `service`, `session`) et un `resolvePrincipal()` compose branche sur `@rasono/auth`.
    - Enrichissement de `src/config.ts` genere pour porter une configuration d'auth de reference validee au startup: tokens statiques optionnels, nom du cookie session et noms de roles.
    - Mise a jour de `packages/create-rasono/template/apps/api/package.json` et de `packages/create-rasono/template/RASONO_AI.md` pour inclure `@rasono/auth` et documenter les variables d'environnement d'auth de reference.
    - Ajout de tests dans `packages/testing/test/testing.test.mjs` pour couvrir le resolver session, la generation du starter API auth-ready et la presence des commentaires d'en-tete anglais dans les fichiers generes.
- **Decisions Techniques :** L'auth de reference reste volontairement simple et basee sur des credentials statiques optionnels, car l'objectif est de fournir une integration officielle lisible et securisee par defaut, pas de figer trop tot un moteur de session complet. Les tokens n'ont pas de fallback implicite, ce qui evite un starter ouvert par accident; sans configuration, les routes protegees restent simplement inaccessibles.
- **Impacts & Dependances :** Fichiers touches : `packages/auth/src/index.ts`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/apps/api/package.json`, `packages/create-rasono/template/RASONO_AI.md`, `packages/testing/package.json`, `packages/testing/test/testing.test.mjs`. Cette tranche reduit le risque de derive entre auth modulaire du coeur, starter officiel et documentation de reference.
- **Prochaines etapes :** Monter maintenant vers une auth de reference plus riche cote policies/tenant isolation, ou renforcer l'OpenAPI et la generation SDK autour des exigences de securite et des schemes d'auth.

### [2026-07-10T11:35:14Z] | Phase 1 socle: modules officiels, HTTP declaratif et config starter
- **Contexte :** Rasono avait deja un bon noyau adapter-first, mais il manquait encore des preuves de "framework credibility" sur trois points clefs de la phase 1: une convention de modules vraiment supportee par la CLI, une surface HTTP/OpenAPI plus explicite, et un starter API avec configuration runtime validee au demarrage.
- **Modifications effectuees :**
    - Ajout de `defineModule()` dans `packages/app/src/module.ts` puis export public depuis `packages/app/src/index.ts` pour officialiser la declaration de modules cote framework.
    - Extension de `packages/app/src/route-definitions.ts` avec `input.cookies`, `response: { status, description, contentType, headers, schema }`, `operationId` et `deprecated`, afin d'ameliorer a la fois le runtime HTTP et la generation OpenAPI.
    - Evolution de `packages/cli/src/generateApi.ts`, `packages/cli/src/generateRpc.ts`, `packages/cli/src/dev.ts` et `packages/cli/src/index.ts` pour supporter `src/modules/*/api/**`, surveiller `src/modules` en dev et documenter cette convention dans la CLI.
    - Refonte du scaffolder `packages/create-rasono/src/index.ts` pour generer un starter API sur une structure modulaire officielle (`src/modules/index.ts`, `system.module.ts`, routes modulees) avec `src/config.ts` pour la validation d'environnement au startup.
    - Ajout de tests dans `packages/testing/test/testing.test.mjs` pour couvrir les cookies + metadonnees de reponse declaratives et la generation des manifests API depuis `src/modules/*/api`, plus mise a jour de `packages/testing/package.json` pour builder la CLI avant test.
- **Decisions Techniques :** La convention de modules a ete mise en place via la CLI et le scaffolder avant d'ajouter des abstractions plus lourdes, car une convention non supportee par l'outillage cree plus de confusion que de valeur. La reponse HTTP declarative reste additive et retro-compatible via `output`, ce qui preserve l'ergonomie actuelle tout en preparant une OpenAPI plus riche. La config runtime reste dans le starter, pas dans le coeur, pour eviter d'imposer prematurement un systeme de configuration au framework tant que la surface n'est pas completement stabilisee.
- **Impacts & Dependances :** Fichiers touches : `packages/app/src/module.ts`, `packages/app/src/index.ts`, `packages/app/src/route-definitions.ts`, `packages/cli/src/generateApi.ts`, `packages/cli/src/generateRpc.ts`, `packages/cli/src/dev.ts`, `packages/cli/src/index.ts`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/RASONO_AI.md`, `packages/testing/package.json`, `packages/testing/test/testing.test.mjs`. Cette tranche reduit le risque de derive entre architecture recommandee, outillage de generation et starters.
- **Prochaines etapes :** Construire sur cette base une auth de reference branchee sur le starter API, puis enrichir la spec OpenAPI avec davantage de metadata de contrat et eventuellement une generation SDK plus stricte.

### [2026-07-10T11:01:08Z] | Auth modulaire et preuve de remplaçabilité Web
- **Contexte :** L'architecture etait deja devenue adapter-first cote backend et partiellement cote frontend, mais il manquait encore deux preuves importantes pour rendre la modularite credible: un module d'auth reutilisable sans couplage transport, et un adapter Web alternatif concret montrant que Rasengan n'est plus une dependance structurelle.
- **Modifications effectuees :**
    - Creation de `packages/auth` avec des resolvers `createBearerPrincipalResolver()`, `createApiKeyPrincipalResolver()` et `composePrincipalResolvers()` pour brancher `resolvePrincipal()` sans dependre de Hono ou d'un middleware specifique.
    - Creation de `packages/web-memory` avec `createMemoryRouterAdapter()` afin de demontrer un adapter Web minimal compatible avec `@rasono/web-core` sans runtime UI externe.
    - Extension de `packages/testing/test/testing.test.mjs` pour couvrir la composition des resolvers auth et la creation d'un routeur alternatif via `createWebRouter(createMemoryRouterAdapter(), ...)`.
    - Mise a jour de `packages/create-rasono/template/RASONO_AI.md` pour documenter les nouveaux packages modulaires et corriger l'exemple `resolvePrincipal()` afin qu'il reflete bien l'API `createApp()` actuelle basee sur `transport`.
    - Consolidation de la resolution monorepo via `tsconfig.base.json` pour inclure `@rasono/auth` et `@rasono/web-memory` dans les paths workspace.
- **Decisions Techniques :** Les resolvers auth ont ete gardes strictement centres sur `Request` + `deps` pour pouvoir etre reutilises par n'importe quel adapter HTTP ou runtime de test. L'adapter Web alternatif reste volontairement minimal: l'objectif est de prouver le contrat et la remplacabilite, pas d'introduire prematurement un second framework frontend lourd a maintenir.
- **Impacts & Dependances :** Fichiers touches : `packages/auth/src/index.ts`, `packages/auth/package.json`, `packages/web-memory/src/index.ts`, `packages/web-memory/package.json`, `packages/testing/test/testing.test.mjs`, `packages/create-rasono/template/RASONO_AI.md`, `tsconfig.base.json`. Risque principal: derive documentaire si les exemples ne suivent pas l'API adapter-first; cette mise a jour reduit ce risque. Validation finale relancee apres documentation pour confirmer build, typecheck et tests sur l'etat reel du repo.
- **Prochaines etapes :** Brancher `@rasono/auth` dans un starter de reference avec policies d'autorisation plus riches, puis ajouter un adapter UI alternatif plus realiste si la strategie multi-framework devient un objectif produit.

### [2026-07-10T10:46:07Z] | Web adapter-first et extraction de @rasono/web-core
- **Contexte :** Le backend etait deja passe en mode adapter-first, mais le frontend restait partiellement couple a Rasengan: le client RPC genere dependait de `@rasono/rasengan` alors qu'il n'utilise qu'un client HTTP generique, et le routeur du starter parlait directement a Rasengan sans frontiere d'adapter locale.
- **Modifications effectuees :**
    - Creation de `packages/web-core` avec `createApiClient()`, `createWebRouter()` et le contrat `RasonoWebRouterAdapter` pour heberger les primitives web generiques hors de tout framework UI.
    - Rebranchement de `packages/cli/src/generateRpc.ts` sur `@rasono/web-core` afin que le client RPC genere ne depende plus de Rasengan.
    - Simplification de `packages/rasengan` en couche de compatibilite qui re-exporte les primitives de `@rasono/web-core`, plutot que de porter sa propre implementation du client API.
    - Mise a jour du starter Web (`packages/create-rasono/template/apps/web/package.json`, `packages/create-rasono/template/apps/web/src/app/app.router.ts`, `packages/create-rasono/template/apps/web/src/app/router.adapter.ts`) pour encapsuler Rasengan dans un adapter local et consommer `@rasono/web-core` comme facade stable.
    - Mise a jour du scaffolder (`packages/create-rasono/src/index.ts`), des paths monorepo (`tsconfig.base.json`) et des tests (`packages/testing/test/testing.test.mjs`) pour integrer `@rasono/web-core`.
- **Decisions Techniques :** Le client RPC a ete sorti dans un package dedie car il n'a rien de specifique a Rasengan et constituait un faux couplage. L'adapter UI concret reste volontairement local au starter Web pour permettre le remplacement futur de Rasengan sans imposer ce runtime dans le monorepo principal ni dans `@rasono/web-core`.
- **Impacts & Dependances :** Fichiers touches : `packages/web-core/*`, `packages/cli/src/generateRpc.ts`, `packages/rasengan/src/index.ts`, `packages/rasengan/src/client.ts`, `packages/rasengan/package.json`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/apps/web/package.json`, `packages/create-rasono/template/apps/web/src/app/app.router.ts`, `packages/create-rasono/template/apps/web/src/app/router.adapter.ts`, `packages/create-rasono/template/RASONO_AI.md`, `packages/testing/package.json`, `packages/testing/test/testing.test.mjs`, `tsconfig.base.json`. Validation executee avec `npm run build && npm run typecheck && npm run test`, succes complet avec 4 tests passes.
- **Prochaines etapes :** Ajouter un template ou package d'adapter UI alternatif pour prouver le remplacement de Rasengan en pratique, puis couvrir le scaffolder Web par des tests automatises de generation par preset.

### [2026-07-10T10:19:34Z] | Architecture adapter-first et decouplage Hono
- **Contexte :** Le cœur Rasono avancait sur la DI, le lifecycle et les contrats de route, mais `@rasono/app` etait encore couple directement a `@rasono/hono` dans ses types publics et dans `createApp()`. Cette dependance contredisait l'objectif de modularite: changer Hono demain aurait force un refactor du cœur applicatif au lieu d'un simple remplacement d'adapter.
- **Modifications effectuees :**
    - Ajout de `packages/core/src/runtime.ts` avec les contrats runtime partages (`RasonoHttpAdapter`, `RasonoHttpApp`, `Principal`, `RasonoLogger`, `RasonoRequestContext`, `ResolvedRequestContext`) puis re-export depuis `packages/core/src/index.ts`.
    - Refactor de `packages/app/src/types.ts`, `packages/app/src/createApp.ts`, `packages/app/src/index.ts` et `packages/app/src/route-definitions.ts` pour supprimer la dependance directe a `@rasono/hono`, introduire `transport: { adapter, options }` et typer `createApp()` autour d'un adapter HTTP generique.
    - Evolution de `packages/hono/src/app.ts`, `packages/hono/src/types.ts` et `packages/hono/src/index.ts` afin de transformer `@rasono/hono` en adapter concret via `createHonoAdapter()`, tout en conservant `createRasonoApp()` pour compatibilite locale.
    - Mise a jour du scaffolder et du starter API (`packages/create-rasono/src/index.ts`, `packages/create-rasono/template/apps/api/src/index.ts`, `packages/create-rasono/template/apps/api/package.json`) pour rendre l'adapter Hono explicite dans les projets generes.
    - Ajout d'une resolution monorepo par sources via `tsconfig.base.json` (`paths`) afin que le typecheck travaille sur les contrats courants des packages au lieu de declarations `dist` stalees.
    - Adaptation du package de test (`packages/testing/src/index.ts`, `packages/testing/test/testing.test.mjs`, `packages/testing/package.json`) pour verifier le mode adapter-first avec Hono en tant qu'implementation pluggable.
- **Decisions Techniques :** Les contrats runtime ont ete places dans `@rasono/core` pour eviter une inversion de dependance entre `@rasono/app` et les adapters. Hono reste l'implementation de reference mais n'est plus une hypothese encodee dans l'API publique de `@rasono/app`. Le frontend conserve deja cette philosophie avec `@rasono/rasengan` comme package separe, ce qui aligne maintenant backend et web sur la meme strategie modulaire.
- **Impacts & Dependances :** Fichiers touches : `packages/core/src/runtime.ts`, `packages/core/src/index.ts`, `packages/app/package.json`, `packages/app/src/types.ts`, `packages/app/src/createApp.ts`, `packages/app/src/index.ts`, `packages/app/src/route-definitions.ts`, `packages/hono/src/app.ts`, `packages/hono/src/types.ts`, `packages/hono/src/index.ts`, `packages/testing/src/index.ts`, `packages/testing/test/testing.test.mjs`, `packages/testing/package.json`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/apps/api/src/index.ts`, `packages/create-rasono/template/apps/api/package.json`, `packages/create-rasono/template/RASONO_AI.md`, `tsconfig.base.json`. Validation executee avec `npm run build && npm run typecheck && npm run test`, succes complet avec 3 tests passes.
- **Prochaines etapes :** Introduire un package `@rasono/auth` adapter-agnostic, puis definir un contrat de web adapter cote UI pour que `@rasono/rasengan` suive exactement la meme logique `adapter-first` que le backend.

### [2026-07-10T05:39:04Z] | Package @rasono/testing et validation runtime reelle
- **Contexte :** Apres DI, lifecycle et `resolvePrincipal()`, il manquait un kit de test officiel pour verifier les integrations sans boot serveur externe. La premiere execution des tests a en outre revele un bug structurel: `resolveAppDeps()` essayait de materialiser des deps `request` pendant le startup.
- **Modifications effectuees :**
    - Creation de `packages/testing` avec `createTestApp()` et `createTestClient()` pour piloter `app.fetch` en tests d'integration sans serveur reseau.
    - Ajout d'une suite `node:test` dans `packages/testing/test/testing.test.mjs` couvrant les overrides, l'injection de `principal`, les routes protegees et le nettoyage des deps request-scoped.
    - Durcissement de `packages/app/src/container.ts` pour que `resolveAppDeps()` ne materialise plus les providers `request`, tout en continuant a resoudre les singletons et les overrides applicables au lifecycle.
    - Mise a jour de `package.json` root et de `packages/testing/package.json` pour fournir une commande de test executable et autonome, ainsi que de `packages/create-rasono/template/RASONO_AI.md` pour documenter `@rasono/testing`.
- **Decisions Techniques :** Le package de test reste intentionnellement leger et repose sur `node:test` + `app.fetch` pour minimiser les dependances et coller au runtime reel. Le bug startup/request-scope a ete corrige dans le container plutot que contourne dans les tests, car il representait une faiblesse d'architecture susceptible de casser des apps reelles des qu'un provider `request` coexiste avec des hooks de lifecycle.
- **Impacts & Dependances :** Fichiers touches : `packages/testing/package.json`, `packages/testing/tsconfig.json`, `packages/testing/src/index.ts`, `packages/testing/test/testing.test.mjs`, `packages/app/src/container.ts`, `package.json`, `packages/create-rasono/template/RASONO_AI.md`. Validation executee avec `npm run typecheck` puis `npm run test`, 3 tests passes.
- **Prochaines etapes :** Ajouter des helpers d'assertion et un `createAuthenticatedTestApp()`, etendre les tests aux schemas `input/output` OpenAPI, puis couvrir le scaffolder avec des tests par preset.

### [2026-07-10T05:31:35Z] | Overrides de dependances et principal resolver officiel
- **Contexte :** La DI et le lifecycle etaient en place, mais il manquait encore deux leviers essentiels pour rendre Rasono exploitable en conditions reelles: des overrides explicites pour les tests/environnements controles, et un point d'entree officiel pour convertir une requete en `principal` securise avant evaluation des policies de route.
- **Modifications effectuees :**
    - Extension de `packages/app/src/container.ts` avec des `overrides` applicables a `resolveAppDeps()` et `createRequestScope()`, ce qui permet d'injecter des doubles de test ou des implementations alternatives sans contourner le container.
    - Evolution de `packages/app/src/types.ts` et `packages/app/src/createApp.ts` pour exposer `overrides` et `resolvePrincipal()` dans `createApp()`, puis appliquer ces extensions a la resolution du scope requete et du lifecycle.
    - Mise a jour de `packages/app/src/index.ts` pour exporter `DepOverrides`, `Principal` et `RasonoLogger`, afin de rendre la nouvelle surface test/auth utilisable depuis `@rasono/app`.
    - Documentation des nouveaux patterns dans `packages/create-rasono/template/RASONO_AI.md`, avec exemples pour `resolvePrincipal()` et les overrides de test.
- **Decisions Techniques :** Les overrides restent des valeurs explicites appliquees au container, pas un systeme opaque de monkey patching, afin de preserver la lisibilite, l'isolement des tests et la surete des environnements de validation. `resolvePrincipal()` est garde au niveau `createApp()` plutot qu'injecte implicitement dans les routes pour conserver une frontiere claire entre authentification HTTP, resolution de contexte et autorisation metier.
- **Impacts & Dependances :** Fichiers touches : `packages/app/src/container.ts`, `packages/app/src/createApp.ts`, `packages/app/src/types.ts`, `packages/app/src/index.ts`, `packages/create-rasono/template/RASONO_AI.md`. Validation executee avec `npm run typecheck` et `npm run build` au root avec succes.
- **Prochaines etapes :** Ajouter un vrai package `@rasono/testing` avec helpers d'override ergonomiques, un test client officiel, puis fournir un module auth de reference (bearer/session/api key) branche sur `resolvePrincipal()`.

### [2026-07-10T05:22:33Z] | DI request-scoped et lifecycle applicatif v1
- **Contexte :** Le contrat de route declaratif etait en place, mais Rasono manquait encore d'un vrai socle d'injection de dependances et d'un cycle de vie applicatif explicite. Sans cela, les services restent trop artisanaux, le cleanup des ressources est fragile et le framework ne donne pas encore de garanties suffisantes en production.
- **Modifications effectuees :**
    - Ajout de `packages/app/src/container.ts` avec `defineDep()`, `createContainer()`, scopes `singleton/request/transient`, detection de dependances circulaires et gestion de `dispose`.
    - Extension de `packages/app/src/types.ts` et `packages/app/src/index.ts` pour exposer `RasonoContainer`, `RasonoLifecycleContext`, `dependencies`, `startup`, `shutdown`, `app.close()` et la nouvelle API DI publique.
    - Refonte de `packages/app/src/createApp.ts` pour construire un container central, fusionner les providers de modules, materialiser les scopes requete, executer les hooks `startup/shutdown` et fermer proprement les singletons.
    - Evolution de `packages/hono/src/app.ts`, `packages/hono/src/types.ts`, `packages/hono/src/index.ts` et `packages/hono/src/background.ts` afin de supporter une resolution de contexte requete avec cleanup deferre apres les background tasks, y compris sous `waitUntil`.
    - Mise a jour des starters/documents (`packages/create-rasono/src/index.ts`, `packages/create-rasono/template/apps/api/src/index.ts`, `packages/create-rasono/template/RASONO_AI.md`) pour montrer `app.close()` sur `SIGINT/SIGTERM` et expliquer la DI/lifecycle.
- **Decisions Techniques :** Le container reste volontairement minimal et base sur des providers par cle, ce qui permet d'introduire des scopes et du cleanup sans casser le modele `ctx.deps` deja deploye. Les providers `request` sont interdits pendant le startup pour eviter des couplages invalides entre cycle de vie applicatif et contexte requete. Le cleanup des scopes requete est volontairement differe apres execution des background tasks pour eviter les use-after-close sur les ressources request-scoped.
- **Impacts & Dependances :** Fichiers touches : `packages/app/src/container.ts`, `packages/app/src/createApp.ts`, `packages/app/src/index.ts`, `packages/app/src/types.ts`, `packages/hono/src/app.ts`, `packages/hono/src/background.ts`, `packages/hono/src/index.ts`, `packages/hono/src/types.ts`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/apps/api/src/index.ts`, `packages/create-rasono/template/RASONO_AI.md`. Validation executee avec `npm run typecheck` et `npm run build` au root avec succes.
- **Prochaines etapes :** Ajouter des helpers d'override pour les tests, exposer un moyen officiel d'injecter le `principal` via auth middleware/providers, puis etendre l'OpenAPI avec une meilleure description des schemas de query/header/cookies et des responses non-JSON.

### [2026-07-10T05:14:34Z] | Contrat de route declaratif v1 et OpenAPI enrichie
- **Contexte :** Rasono etait encore trop proche d'un socle Hono outille. Il manquait une primitive backend de plus haut niveau pour exprimer proprement validation d'entree, contrat de sortie, metadonnees de securite et documentation OpenAPI sans plomberie repetitive.
- **Modifications effectuees :**
    - Extension de `packages/core/src/validation.ts` avec `defineSchema()` et un champ `openapi` optionnel sur `Schema` pour porter a la fois validation runtime et description documentaire.
    - Refonte de `packages/app/src/route-definitions.ts` pour introduire `defineRoute()`, `installApiRoute()`, `input` (body/query/params/headers), `output`, `auth`, validation unifiee des entrees, verification d'autorisation de base et serialisation de sortie.
    - Mise a jour de `packages/app/src/index.ts` pour exposer proprement la nouvelle surface publique tout en conservant `defineApi()` comme alias de compatibilite.
    - Evolution de `packages/cli/src/generateApi.ts` afin que le codegen installe les routes via `installApiRoute()` et derive une OpenAPI plus riche a partir du contrat de route, y compris request body, output schema, path params et schemes de securite.
    - Alignement du scaffolder et des exemples (`packages/create-rasono/src/index.ts`, `packages/create-rasono/template/apps/api/src/api/hello.ts`, `packages/create-rasono/template/apps/api/src/api/http-error.ts`, `packages/create-rasono/template/RASONO_AI.md`) sur `defineRoute()` et `defineSchema()`.
- **Decisions Techniques :** Le premier slice cible volontairement le contrat HTTP plutot que la DI complete ou le lifecycle avance. C'est le point de levier le plus rentable pour rendre le framework plus declaratif, mieux documente et plus difficile a mal utiliser, tout en restant compatible avec les handlers existants qui retournent deja une `Response`.
- **Impacts & Dependances :** Fichiers touches : `packages/core/src/validation.ts`, `packages/core/src/index.ts`, `packages/app/src/route-definitions.ts`, `packages/app/src/index.ts`, `packages/cli/src/generateApi.ts`, `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/apps/api/src/api/hello.ts`, `packages/create-rasono/template/apps/api/src/api/http-error.ts`, `packages/create-rasono/template/RASONO_AI.md`. Validation executee avec `npm run typecheck`, puis `npm run build` au root avec succes.
- **Prochaines etapes :** Ajouter un vrai container DI request-scoped, etendre la securite runtime au dela des roles simples, enrichir la generation OpenAPI avec schemas de query/header plus descriptifs, puis fournir un kit de tests avec dependency overrides.

### [2026-07-10T04:45:57Z] | Presets de scaffold et modularite d'installation
- **Contexte :** Le scaffolder devait devenir plus professionnel et modulaire: permettre a l'utilisateur de choisir un type d'application au demarrage, avec une version interactive expliquee et un mode rapide 100% pilotable par flags.
- **Modifications effectuees :**
    - Refonte de `packages/create-rasono/src/index.ts` pour ajouter les presets `landing-page`, `api-only`, `simple-crud`, `full-app` et `custom`.
    - Ajout d'un mode interactif leger base sur `readline/promises`, sans dependance externe de prompts, avec descriptions claires des profils et composition fine en mode `custom`.
    - Ajout des flags de scaffold `--preset|--type|--kind`, `--api`, `--web`, `--actions`, `--swagger`, `--rpc`, `--yes`, `--no-install`, `--pm`.
    - Specialisation du scaffold selon le profil: suppression des apps inutiles, retrait des dependances optionnelles, adaptation des fichiers `src/index.ts`, pages d'accueil et exemples CRUD.
    - Allègement des generateurs `api/actions/rpc` pour que les fichiers generes vides n'importent plus de packages optionnels.
- **Decisions Techniques :** Les presets restent des compositions declaratives de briques plutot que des templates dupliques, ce qui preserve la maintenabilite et permet a un client de n'installer que le strict necessaire. Les generateurs vides ont ete rendus autonomes afin qu'un preset Web-only n'impose plus `@rasono/app`, `@rasono/actions` ou `@rasono/rasengan`.
- **Impacts & Dependances :** Fichiers touches : `packages/create-rasono/src/index.ts`, `packages/cli/src/generateApi.ts`, `packages/cli/src/generateActions.ts`, `packages/cli/src/generateRpc.ts`, `packages/app/src/index.ts`, `packages/app/package.json`, `packages/create-rasono/template/apps/api/package.json`, `packages/create-rasono/template/apps/api/src/api/http-error.ts`, `packages/create-rasono/template/RASONO_AI.md`. Validation executee sur un scaffold interactif `landing-page` et un scaffold par flags `api-only`, tous deux installes et buildes avec succes.
- **Prochaines etapes :** Ajouter des tests automatises du scaffolder par preset, puis etendre les presets avec davantage de starters metier si necessaire.

### [2026-07-10T04:30:30Z] | Validation scaffold et runtime Node
- **Contexte :** La verification complete du monorepo et du scaffold `create-rasono` a revele des echecs reels d'installation, de build template et de runtime API, en particulier sur la resolution locale des packages `@rasono/*`, le build Rasengan en workspace hoiste, et le support Node sans `ExecutionContext`.
- **Modifications effectuees :**
    - Renforcement de `packages/create-rasono/src/index.ts` pour patcher automatiquement les dependances `@rasono/*` en specs locales `file:` depuis le repo source et injecter des `overrides`/`resolutions` au root du scaffold.
    - Stabilisation du template `create-rasono` avec versions explicites `0.1.0` pour les packages internes, `rootDir` explicite cote API, imports ESM `.js` vers les manifests generes, et scripts Web compatibles avec le layout hoiste de Rasengan.
    - Ajout de `apps/web/scripts/link-rasengan.mjs` pour creer un lien local vers `rasengan` apres installation et aligner le template avec les attentes internes de Rasengan.
    - Durcissement de `packages/hono/src/app.ts` pour rendre l'acces a `executionCtx.waitUntil` sur et compatible Node, sans casser le chemin edge.
- **Decisions Techniques :** Le mode local du scaffolder est detecte automatiquement pour permettre une validation repo-source sans publication npm, tout en conservant des versions fixes pour le cas publie. Le contournement Rasengan privilegie des scripts explicites et un lien local maitrise plutot qu'un hack global du gestionnaire de paquets. Le fallback `waitUntil` preserve la compatibilite edge tout en eliminant les 500 systemiques sous Node.
- **Impacts & Dependances :** Fichiers touches : `packages/create-rasono/src/index.ts`, `packages/create-rasono/template/apps/api/package.json`, `packages/create-rasono/template/apps/api/src/index.ts`, `packages/create-rasono/template/apps/api/tsconfig.json`, `packages/create-rasono/template/apps/web/package.json`, `packages/create-rasono/template/apps/web/scripts/link-rasengan.mjs`, `packages/hono/src/app.ts`. Le scaffold genere maintenant s'installe localement depuis le repo, build les apps API/Web, et demarre l'API avec succes sous Node.
- **Prochaines etapes :** Ajouter des tests automatises cibles pour le scaffolder et le runtime Node, puis durcir la couche RPC generee et la validation des server actions si le scope continue.
